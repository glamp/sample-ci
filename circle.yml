machine:
  services:
    - docker
test:
  override:
    - ci/release.sh 1
    - ci/release.sh 10
    - ci/release.sh 100
    # case $CIRCLE_NODE_INDEX in 0) ci/release.sh 1 ;; 1) ci/release.sh 2;; esac:
    #   parallel: true
